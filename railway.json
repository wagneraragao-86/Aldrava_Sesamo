{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build -w @aldrava/shared && npm run prisma:generate -w @aldrava/backend && npm run build -w @aldrava/backend"
  },
  "deploy": {
    "startCommand": "npm run prisma:migrate -w @aldrava/backend && npm run seed -w @aldrava/backend && npm run start -w @aldrava/backend",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
